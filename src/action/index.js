export const increment = () => ({ type: "INCREMENT" });

// export function increment() {
//   this.dispatch({ type: "INCREMENT" });
// }

// action types for api

 //export const ActionTypes = ({ type: "SET_POKEMON" });
